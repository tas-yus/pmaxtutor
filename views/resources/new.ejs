<% include ../partials/header %>
    <h1 id="form-header">Add Resource</h1>
    <form action="/courses/<%= courseCode %>/parts/<%= partCode %>/videos/<%= videoCode %>/resources" enctype="multipart/form-data" method="POST"  id="regular-form">
        <div class="form-group">
            <input class="form-control" type="text" name="title" placeholder="ชื่อลิงค์">
        </div>
        <div class="form-group">
            <input type="file" name="file">
        </div>
        <button class="btn btn-block btn-primary">Add</button>
    </form>
<% include ../partials/footer %>
